name: isolate_handler_example
description: Demonstrates how to use the isolate_handler plugin.

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^01.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  isolate_handler:
    path: ../

  flutter_lints: ^1.0.0
  path_provider: ^2.0.1

flutter:
  uses-material-design: true
